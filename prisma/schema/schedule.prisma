model Schedule {
    id            String   @id @default(uuid())
    startDataTime DateTime
    endDateTime   DateTime
    startTime     String
    endTime       String
    createdAt     DateTime @default(now())
    updatedat     DateTime @updatedAt

    doctorScheduls DoctorSchedules[]
    appoinments    Appoinment[]

    @@map("schedules")
}
